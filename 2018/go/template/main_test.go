package main

import (
	"testing"
)

func TestSolveInput1(t *testing.T) {
	input := 
	result := solve(input)
	answer := 
	if result != answer {
		t.Fatalf(`solve is wrong. Wanted %d, got %d`, answer, result)
	}
}
